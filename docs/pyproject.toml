[project]
name = "ymir-docs"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "markdown-callouts>=0.4.0",
    "mkdocs-material>=9.6.15",
    "pygments",
]

# The wiki no longer contains KDL code fences (everything is Lua now), so this
# pin is obsolete.
# FIXME: drop this [tool.uv.sources] block once uv.lock has been regenerated.
[tool.uv.sources]
pygments = { git = "https://github.com/chinatsu/pygments", rev = "0f0b0d4da2839e1285881389155bb4605a0a6dc4" }
